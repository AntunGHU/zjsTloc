// 7'11

//* app

//An empty array:
let todoList = [];

//Array of strings:
let shoppingList = ['cereal', 'cheese', 'ice'];
console.log(shoppingList)

// Array of numbers:
let lotto = [45, 12, 23, 25, 34];
console.log(lotto)

// Array of multiple types:
let myCollection = [12, 'dog', true, null, NaN];
console.log(myCollection)

//* video

// Arrays: ordered collections of values (*list of comments, *collection of levels in a game, *songs in a playlist - spotify)